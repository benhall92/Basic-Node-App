<!DOCTYPE html>
<html>
  <head>
    <title> - Profile</title>
    <% include ./partials/head %>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col">
          <div class="jumbotron">
            <h1>Profile</h1>
            <% if(user.local) { %>
              Logged in
            <% } %>
            <p><strong><%= user.local.email %></strong></p>
            <a href="/logout">Logout</a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
